# Sistema CRUD con Python, Flask y MySQL

Este proyecto es un sistema CRUD (Crear, Leer, Actualizar, Eliminar) con:

- **Python + Flask** para el backend
- **MySQL** como base de datos
- **HTML y CSS** para la interfaz

## Instalación

1. Clonar el repositorio:
```bash
git clone https://github.com/canelajoel3/sistema-crud-python-html-css-mysql-flask.git
